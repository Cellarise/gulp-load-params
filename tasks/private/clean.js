/* jslint node: true */
"use strict";

/**
 * A gulp build task to clean (delete) build and report folders.
 * @alias tasks:clean
 */
module.exports = function(gulp) {
    gulp.task('clean', function () {

    });
};
